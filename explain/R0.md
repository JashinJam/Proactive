# R0 技术讲解：无显式计划的零样本基线

> 更新时间：2026-07-15  
> 对应实验：20260713_internvl35_1b_no_plan_r0  
> 实验状态：完成并冻结  
> 说明：本文用于通俗理解。正式事实和数值以实验报告、冻结产物及官方 scorer 为准。

## 1. 一句话理解 R0

R0 在每个视频区间结束时，把“截至当前的最多 32 帧画面、官方任务规则、用户任务和当前允许看到的历史对话”交给 InternVL3.5-1B，让模型自由生成 $interrupt$... 或 $silent$，然后把生成文本整理成合法提交格式。

完整过程是：

~~~text
原始 session
  -> 按 chunk 顺序处理
  -> 从当前绝对时间区间抽取最多 16 帧
  -> 与历史帧合并并均匀压缩到最多 32 帧
  -> 构造 system、query、dialog 消息
  -> 转换成视觉张量和文本 token
  -> InternVL 贪心生成回答
  -> 规范化 interrupt/silent 标签
  -> 写入 predictions.jsonl
~~~

R0 是 no-plan、zero-shot、frozen baseline：

- no-plan：没有额外提供明确的任务步骤、当前步骤或完成状态；
- zero-shot：没有用这批数据的标签训练或微调模型；
- frozen：模型权重和推理配置固定不变；
- baseline：后续方法必须与之比较的基础参照。

这不代表模型完全不会在内部推测步骤，只表示工程上没有单独维护可读取、可更新的显式计划状态。

## 2. 原始数据哪些真正进入模型

一个 session 是一段完整任务视频，一个 chunk 是其中一次需要作决定的时间区间。

| 原始字段 | 工程用途 | 模型是否直接看到 |
|---|---|---|
| video_path | 找到视频文件 | 否 |
| video_intervals[i] | 决定从哪里抽帧 | 只看到抽出的画面，看不到时间数值 |
| query | 用户的总体任务请求 | 是 |
| dialog[i] | 当前时刻允许看到的历史对话快照 | 是，经过裁剪 |
| domain、task | 按领域和任务统计结果 | 否 |
| answers | 标准答案，最后评分使用 | 推理时不读取 |

因此，模型不知道当前是第几个 chunk、视频时间是多少、两个 interval 之间有多长空白，也没有明确标记哪些帧属于当前 interval。

源 JSONL 物理上包含 answers，但生成前会严格限制代码访问范围。R0 的消息构造、抽帧和模型调用不会读取 row["answers"]。完整预测冻结后，官方评分器才读取标准答案。

## 3. 视频输入如何构造

对于第 i 个绝对时间区间 [start, end]，R0 调用官方 extract_frames()：

~~~text
start_frame = int(start * FPS)
end_frame   = min(int(end * FPS), 视频最后一帧)
n           = min(16, end_frame - start_frame + 1)
step        = (end_frame - start_frame) / n
frame[k]    = int(start_frame + k * step)
~~~

FPS 是 frames per second，即视频每秒帧数。OpenCV 读出的画面采用 BGR 色彩顺序，代码将其转换成普通 RGB，再转换为 PIL Image。

每次作决定前，当前帧会加入累计视觉记忆：

~~~text
cumulative_frames += current_interval_frames
model_frames = uniform_subsample(cumulative_frames, max_frames=32)
~~~

典型帧数变化如下：

~~~text
chunk 0：16 帧 -> 输入 16 帧
chunk 1：累计 32 帧 -> 输入 32 帧
chunk 2：累计 48 帧 -> 均匀压成 32 帧
chunk 9：累计约 160 帧 -> 均匀压成 32 帧
~~~

超过 32 帧时使用：

~~~text
selected[k] = frames[int(k * N / 32)]
~~~

这种方法均匀覆盖整个已看过程，不是只保留最近 32 帧。它的缺点是当前动作没有更高权重，而且不保证最后一帧一定被保留。所有选中帧会作为“一段视频”输入，没有 interval 分隔符或时间 gap 标记。

实际 9,935 个 chunk 中，9,230 个输入了 32 帧，696 个输入了 16 帧，另有 9 个因为区间极短或帧索引去重而使用其他数量。

## 4. 文本消息如何构造

第 i 个 chunk 的模型消息是：

~~~text
system：官方 proactive 任务规则
user：session query
history：dialog[i][1:] 的末尾 4 个原始 turn，随后规范角色并跳过空文本
~~~

术语解释：

- system：系统规则，优先告诉模型任务和输出要求；
- user：用户消息；
- assistant：此前的助手回答；
- turn：一条 user 或 assistant 消息；
- history：当前允许看到的历史对话；
- prompt：送给模型的完整文字指令和上下文。

dialog[i] 不是单独一句话，而是第 i 个决策时刻的完整可见对话快照。dialog[i][0] 通常重复 query，因此 R0 无条件去掉第一条，再截取末尾 4 条。未知 role 会按 user 处理，空文本会被跳过。

一个重要边界是：历史 assistant 内容来自官方 dialog[i]，不是 R0 自己先前的预测。因此 R0 是官方协议下的逐 chunk 条件推理，不是把自身输出不断写回的 closed-loop rollout。closed-loop 表示模型自己的历史决定会影响后续输入。

## 5. 官方 system prompt 要求什么

官方 prompt 告诉模型：

1. 它正在观看用户执行程序性任务的第一视角视频；
2. 视频被分成约 8 秒的短区间；
3. 每个区间结束后决定说话还是沉默；
4. 说话必须以字面标签 $interrupt$ 开头；
5. 沉默必须只输出 $silent$；
6. 用户提问、动作需要纠正或下一步指导及时有用时可以说话；
7. 没有有用内容时保持沉默。

R0 没有根据验证集修改这段 prompt。

## 6. Chat template 如何包装消息

chat template 是模型专用的对话模板。它会把普通消息转换为：

~~~text
<|im_start|>system
[官方 proactive prompt]
<|im_end|>
<|im_start|>user
<video>
How do I decorate a notebook cover with stickers?
<|im_end|>
<|im_start|>assistant
[此前允许看到的助手回答]
<|im_end|>
<|im_start|>assistant
~~~

最后一个空的 assistant 开头表示“现在轮到模型继续生成”。

第一个 user 消息同时包含 video 占位符和 query。processor 随后把 video 占位符展开为：

~~~text
Frame1: <img><IMG_CONTEXT>重复256次</img>
Frame2: <img><IMG_CONTEXT>重复256次</img>
...
~~~

这些 IMG_CONTEXT 不是自然语言单词，而是等待图像向量填入的视觉位置。

## 7. 图像如何变成视觉 token

每一帧先调整为 448 x 448，转换为 RGB 浮点值，除以 255，并使用固定均值和标准差归一化。

视觉编码过程是：

~~~text
448 x 448 图像
  -> 按 14 x 14 划分
  -> 32 x 32 = 1024 个图像 patch
  -> 视觉编码器提取特征
  -> pixel shuffle 以 0.5 比例压缩空间网格
  -> 16 x 16 = 256 个视觉向量
  -> multimodal projector 投影到语言模型的 1024 维空间
~~~

patch 是图像小块。pixel shuffle 在这里把相邻空间特征重排并下采样。projector 是投影层，它把视觉特征维度转换成语言模型能使用的维度。

上游 checkpoint 中的视频预处理配置错误地写有 384 x 384。384 会形成不适合 0.5 pixel shuffle 的 27 x 27 patch 网格，首次 smoke 在模型内部 reshape 时失败。R0 将视频尺寸固定为视觉塔真实要求的 448 x 448。这是配置一致性修复，不是根据标签调参。

## 8. 最终送入模型的张量

tensor 是多维数字数组。以第一个真实 session 为例：

| Chunk | pixel_values | input_ids | 视觉占位 token |
|---|---|---:|---:|
| 0 | [16, 3, 448, 448] | [1, 4389] | 4,096 |
| 2 | [32, 3, 448, 448] | [1, 8688] | 8,192 |

含义是：

- 16 或 32：帧数；
- 3：RGB 三个颜色通道；
- 448 x 448：每帧高宽；
- input_ids：文字、标签和特殊符号转换成的整数编号；
- attention_mask：与 input_ids 同长度，标明哪些位置有效；
- token：模型处理文本的基本单位，可能是单词、半个单词或标点，不等于完整英文单词。

每帧有 256 个视觉位置，所以 16 帧对应 4,096 个视觉 token，32 帧对应 8,192 个。视觉编码器生成的向量会替换这些占位 token 的普通词嵌入。

## 9. 模型怎样生成答案

R0 使用 InternVL3.5-1B-HF，参数总数为 1,060,897,792，没有额外 planner、adapter、LoRA、classifier 或 decision head。

冻结生成配置：

| 设置 | 含义 |
|---|---|
| max_new_tokens = 64 | 最多新生成 64 个 token |
| do_sample = false | 不随机采样 |
| greedy decoding | 每一步选择当前得分最高的下一个 token |
| use_cache = true | 在当前回答内部使用 KV cache |
| BF16 | 使用 16 位脑浮点数降低显存和计算成本 |
| SDPA | PyTorch 的高效 scaled dot-product attention |
| eval() | 关闭训练时的随机行为 |
| inference_mode() | 不计算梯度 |

KV cache 缓存本次回答已经计算过的注意力 key/value，避免每生成一个新 token 都从头计算。但它只在一次回答内部使用，下一个 chunk 会重新处理整个 prompt 和全部选中帧，不存在跨 chunk 的模型内部记忆。

模型生成完成后，只截取 prompt 之后的新 token，移除特殊 token，再去掉首尾空白，得到 raw response。

## 10. 原始输出如何变成正式答案

raw response 是模型原始生成文本。canonicalization 是将它转换为合法提交格式的过程。

| Raw response | 最终 answer | 原因 |
|---|---|---|
| $interrupt$Add the sticker. | 原样保留 | 已合法 |
| $interrupt$ | $interrupt$Please continue with the next step. | interrupt 后没有正文 |
| $silent$ because... | $silent$ | 去掉 silent 后缀 |
| Add the sticker. | $silent$ | 不以合法 interrupt 标签开头 |

官方评分器只执行：

~~~text
忽略开头空白后，是否以完全相同的 $interrupt$ 开头？
是 -> interrupt
否 -> silent
~~~

检查区分大小写。$Iinterrupt$、先写解释再写 $interrupt$、只写自然语言指导，都会被当成 silent。当前官方主指标只评判 interrupt/silent 标签，不评价 interrupt 后面的指导内容质量。

## 11. 一个真实失败例子

首个真实样本的任务是用贴纸装饰笔记本封面。chunk 0 输入了 0.0 到 2.0 秒的 16 帧和用户 query。

模型原始输出：

~~~text
Place the stickers on the cover of the notebook.
~~~

规范化结果：

~~~text
answer: $silent$
normalization: malformed_response_scored_as_silent
~~~

模型语义上明显想提供指导，但漏写 $interrupt$，所以官方按 silent 评分。该 chunk 的 gold 是 interrupt，因此形成 FN，即 false negative，中文是“漏报”。

全量有 633 个这种非法 raw response，其中 628 个位于每个 session 的第一个 chunk，630 个对应 gold interrupt。这说明 R0 的一部分损失来自输出协议，而不是单纯没有产生有用内容。

## 12. 正式输出和内部诊断输出

内部 session_records.jsonl 保存：

~~~json
{
  "chunk_index": 0,
  "interval": [0.0, 2.0],
  "current_interval_frames": 16,
  "model_input_frames": 16,
  "raw_response": "Place the stickers...",
  "answer": "$silent$",
  "normalization": "malformed_response_scored_as_silent"
}
~~~

正式 predictions.jsonl 每行只包含：

~~~json
{
  "video_path": "0028bee556249cd3.mp4",
  "answers": ["$silent$", "$silent$"]
}
~~~

每个 session 恰好一行，顺序必须与输入一致，answers 数量必须与 video_intervals 数量一致。

## 13. R0 的因果性

causal 表示当前决策不能使用未来信息。R0 在 chunk i：

- 只在到达当前循环时解码 interval i；
- 只累计 0 到 i 的帧；
- 只使用 dialog[i]；
- 不读取 dialog[i+1]；
- 不读取当前或未来 answers；
- 不生成未来视频摘要或未来步骤。

整个 JSON 行虽然已加载到内存，静态校验也会检查所有 interval 的结构，但模型输入不使用未来字段内容。

## 14. 工程执行和复现

700 个 session 被按原始顺序切成 8 个连续 shard。shard 是分片，每个 worker 负责一段完整 session 范围。单个 session 内仍严格按 chunk 顺序运行。

每完成一个 session，程序执行 write、flush、fsync，保证断电或中断后已完成记录真正落盘。合并时检查：

- 8 个 shard 是否齐全；
- global input_index 是否精确覆盖 0 到 699；
- 是否有重复、缺失或乱序；
- 每个 video_path 和 answers 数量是否匹配；
- 配置、模型和数据指纹是否一致。

所有检查通过后才生成正式 predictions，并调用一次官方全量 scorer。

## 15. R0 最终告诉了我们什么

R0 的官方 Macro F1 是 0.4630。它确实学到了一些输入相关的区别，因为高于永远 silent 和永远 interrupt 两个常数策略；但它明显偏向沉默：

- gold interrupt 比例：53.87%；
- predicted interrupt 比例：29.34%；
- FN：3,811；
- interrupt recall：0.2879。

它暴露了四类后续问题：

1. 格式问题：自然语言指导经常缺少 $interrupt$；
2. 决策校准问题：模型整体过于保守；
3. 初始状态问题：首 chunk 几乎不知道应该开始指导；
4. 显式状态问题：没有清楚表示当前步骤、完成证据、错误证据和下一步。

从建模角度看，R0 是“用生成式聊天模型完成二分类”。它没有独立 decision head，而是让语言模型通过生成特定字符串表达二元决策，因此可能同时发生“判断错误”和“意思正确但格式错误”。

## 16. 术语速查

| 英文术语 | 中文解释 |
|---|---|
| session | 一段完整任务视频样本 |
| chunk / interval | 一次作决定对应的视频时间区间 |
| query | 用户提出的总体任务 |
| dialog history | 当前允许看到的历史对话 |
| prompt | 交给模型的完整文字上下文 |
| processor | 负责整理图像、文字和张量的预处理器 |
| tokenizer | 把文本拆成 token 并转换成数字编号的组件 |
| token | 模型处理文本或视觉位置的基本单位 |
| tensor | 多维数字数组 |
| vision tower | 提取图像特征的视觉编码器 |
| projector | 把视觉特征转换到语言模型维度的投影层 |
| greedy decoding | 每步选择最高分 token 的确定性生成 |
| KV cache | 缓存本次生成中已算注意力结果的机制 |
| canonicalization | 把原始输出整理成标准格式 |
| malformed response | 不符合规定标签格式的原始回答 |
| fallback | 遇到异常格式时采用的默认处理 |
| zero-shot | 没有针对当前任务标签训练 |
| frozen | 权重和设置固定不更新 |
| no-plan | 没有显式外部计划或程序状态 |
| causal | 当前决策不使用未来信息 |
| shard | 为并行处理而切分的数据分片 |

## 17. 对应材料

- [R0 正式报告](../reports/20260713_internvl35_1b_no_plan_r0.md)
- [R0 核心流程](../src/proactive_r0/core.py)
- [InternVL 适配器](../src/proactive_r0/internvl.py)
- [R0 冻结配置](../configs/r0_internvl35_1b_no_plan.json)
- [R0 正式产物](../output/experiments/20260713_internvl35_1b_no_plan_r0/)

